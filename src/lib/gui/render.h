#pragma once

// this library might become "graphics" instead of gui?

namespace cimbar {

	void render(const cimbar::image& img)
	{
		// if cv::mat, just use highgui?
		// if raylib, some magic?
	}

}
